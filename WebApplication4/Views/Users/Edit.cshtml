@model User

<div class="container">
    <div class="row">
        <form method="post" asp-action="Update">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <fieldset>
                <legend>User details</legend>
                <div class="mb-3">
                    <label for="name-input" class="form-label">Name</label>
                    <input asp-for="Name" type="text" value="@Model.Name" id="name-input" class="form-control" placeholder="Name">
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <label for="age-input" class="form-label">Age</label>
                    <input asp-for="Age" type="text" value="@Model.Age" id="age-input" class="form-control" placeholder="Age">
                    <span asp-validation-for="Age" class="text-danger"></span>
                </div>
            </fieldset>
            <input type="submit" value="Save" class="btn btn-primary" />
            <a asp-action="Index" class="btn btn-secondary">Back to List</a>
        </form>
    </div>
</div>
@section Scripts
{
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
